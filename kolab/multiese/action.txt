a % 2 == 0   @@not.if
aが偶数かどうか
aが奇数でないかどうか

a == 0   @@not
aが0かどうか
# aが0と等しいかどうか

a != 0   @@not
aが0でないかどうか

a < 100  @@if @@not.if @@while @@not.while @@let
aが100より小さいかどうか

df['A']  @@check
@type(df, データフレーム)のカラム'A'
